[package]
name = "GUI"
version = "0.1.0"
edition = "2021"

[dependencies]
adw = { version = "0.7.1", package = "libadwaita" }
gtk = { version = "0.9.5", package = "gtk4" , features = ["blueprint"]}
shared={path="../shared", features = ["std"]}
[build-dependencies]
glib-build-tools = "0.20.0"